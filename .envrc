# Stub — the old .envrc used export_var/end_env from a custom direnv lib that
# no longer exists (and carried plaintext tokens, since rotated). Per-project
# env now belongs in a .env file (gitignored) or a nix devshell.
echo "direnv: 👋 $(basename "$PWD") environment loaded"

# uncomment when this repo grows a devshell:
# use flake
dotenv_if_exists .env
