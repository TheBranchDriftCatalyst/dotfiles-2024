# direnv — may hold tokens; never track
.envrc
.direnv/

# nix build outputs
result
result-*

# home-manager backups
*.hm-bak

# OS noise
.DS_Store

# root-owned droppings from sudo darwin-rebuild/nh runs in-repo
.cache/

# .scratch is an ARCHIVE of retired scripts — sources welcome, compiled
# binaries are not (this repo once carried 39MB of caches; never again)
.scratch/bin/catalystTest
.scratch/bin/clean_branches
.scratch/bin/multiProcCLI
.scratch/bin/stor
.scratch/bin/unstor
.scratch/bin/wipctl
