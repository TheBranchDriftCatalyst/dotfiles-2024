<!-- OPERATOR NOTE: placeholder. The real global prompt lived on the old
     hdaniels machine at ~/.claude/CLAUDE.md but the folder dump dropped every
     top-level file (see home/claude/hooks/HOOKS-LIFECYCLE.md, which references
     it). Re-grab from the old box and replace this file's contents — the
     symlink means no rebuild needed. Edits via `/memory` land here too. -->
