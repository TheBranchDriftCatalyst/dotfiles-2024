# kexec + the other keepers from the old bin folders (now archived in
# .scratch) get their modular repackaging here — the retired bin-dir
# pattern's replacement (see catalyst.nix NOTE). Empty until that work
# starts; imported from nowhere yet.
_: { }
